import styled from 'styled-components';

export const StyledContactBadge = styled.div`
    font-size: var(--font-18);
    color: var(--gray-mute);
    font-weight: 700;
    margin-top: 15px;
`;
