import styled from 'styled-components';

export const StyledDateBadge = styled.span`
    display: inline-block;
    padding: 5px 10px;
    font-size: var(--font-16);
    font-style: italic;
    color: var(--gray-mute);
`;
